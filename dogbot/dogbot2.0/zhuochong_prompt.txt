请使用pyqt实现一个桌面宠物：
1、可以通过替换gif图片的方式，替换皮肤。
2、通过全局快捷键CTRL+CTRL可以唤醒宠物，启动运行。
3、右键点击宠物，可以关闭宠物，可以替换皮肤。
4、鼠标放到宠物上，宠物可以做出对应的表情。
5、第一个宠物形象是线条小狗。

请实现上述桌面宠物的所有功能。

右键点击宠物，宠物关闭了，但是程序并没有退出。

期望：右键点击宠物，关闭宠物，退出程序。

请优化。

1、小狗的形象不是很好，看起来像个老鼠，请优化下小狗的形象，一定是个小狗。
2、增加小狗的各种动作的动画：跳舞、摇头、跑步、吃饭、睡觉等小狗的日常卖萌的动画。
请优化。

在文件夹gifs 放了很多gif图片，请增加功能：
1、宠物随机显示播放gif图片。
2、宠物随机到桌面（不包含任务栏）的四个角和四个边的中点播放。

请增加以上功能。

1、将绘制的小狗去掉，启动后默认播放 begin.gif
2、单击左键时，随机播放 click1.gif和click2.gif 这两个图片。
3、鼠标悬停在宠物上，随机播放 left.gif和right.gif 这两个图片。
4、其他情况，随机播放 happy开头的gif图片。
请优化以上功能。


请将gifs 文件夹下的所有gif图片读取后保存成变量，一个图片对应一个变量，变量名称是图片名称的转换：小写字母变成大写，文件名称中的'.'替换成成'_'，比如 click1.gif对应的变量是CLICK1_GIF ,
然后将所有gif图片转换后的变量保存在gifs.py文件中，然后使用时按照变量读取转换成gif图片后使用。

请按照上述描述优化。

理解错我的意思了：gifs.py中 不是将图片的路径保存成变量，而是将图片的内容保存成变量，然后使用时将变量的值再转换成gif图片来使用。
最终的效果时，程序运行时不再依赖gifs文件夹里的图片，而是完全从gifs.py中读取图片数据。

请按照上述思路继续优化。




1、能否将pyqt6切换成pyqt5，并且直接点击运行按钮会报错，请彻底修复这个问题。
2、程序打开后直接崩溃了，保存成base64的数据是不是存储成了静态图片了？请分析根本原因，并才彻底解决这个问题，下面的方法可以参考：用 Python 的 open(…, 'rb') 读取全部字节，存入变量 self.gif_data（bytes 类型）。
3、新增文件夹gifs2 文件夹下的所有gif图片读取后保存成变量，一个图片对应一个变量，变量名称是图片名称的转换：开头加个’2_‘，然后小写字母变成大写，文件名称中的'.'替换成成'_'，比如 click1.gif对应的变量是2_CLICK1_GIF ,
然后将所有gif图片的转换成二进制数据后存放到变量中，将变量保存在gifs2.py文件中，然后使用时按照变量读取后再转换成gif图片后使用。
gifs2.py的变量使用方式如下：
①将绘制的小狗去掉，启动后默认播放 2_BEGIN_GIF
②单击左键时，随机播放 2_CLICK1_GIF和2_CLICK2_GIF 这两个图片。
③鼠标悬停在宠物上，随机播放 2_LEFT_GIF和2_RIGHT_GIF 这两个图片。
④其他情况，随机播放 2_HAPPY开头的gif图片。
4、右键点击宠物可以切换风格，一共两个风格：黄色小狗、白色小狗，两种风格。
①默认是白色小狗；
②白色小狗 风格时，使用gifs2.py中的图片变量；黄色小狗 风格时，使用gifs.py中的图片变量。
③风格切换后，实时生效。
5、程序启动后，默认宠物不弹出，只有当按下全局快捷键CTRL+CTRL后，宠物才会显示出来。

请彻底解决上面的1和2的问题，增加3和4的功能。



一、直接点击运行按钮会报错，请彻底修复这个问题。
二、增加新功能：
注册全局快捷键SHIFT+SHIFT可以将宠物钉住：
1、宠物不可见时，这个快捷键不做任何处理。
2、宠物可见时：
①如果宠物尚未被钉住，宠物的活动范围是全屏幕区域时，通过全局快捷键SHIFT+SHIFT可以将宠物钉住，钉在在屏幕右下角，宠物只能在屏幕右下角活动。
②如果宠物已经被钉住，宠物的活动范围是在屏幕右下角时，通过全局快捷键SHIFT+SHIFT可以将宠物解除锁钉，宠物的活动区域重新恢复成全屏幕活动。

请彻底解决问题一，并增加二中描述的功能，不要影响已有功能。


1、main文件中39行的托盘的菜单增加  切换风格，然后可以切换 pet_windows文件中117行到128行代码的白色小狗和黄色小狗选项的功能。
2、main文件中33行使用的托盘图标，使用gifs2文件夹下的begin.gif中的图片，读取后存为base64的数据为变量，使用时将该变量转换成png图片，用于作为托盘图标，使用时不依赖gifs2文件夹。

请做以上优化。


保持功能不变，不影响已有的所有功能，请做以下优化，目标是优化CPU消耗，将CPU消耗减少到最低：
1、全局快捷键：CTRL+CTRL （双击CTRL）修改成CTRL（单击CTRL），对应的功能不变。
2、全局快捷键：SHIFT+SHIFT（双击SHIFT）修改成SHIFT（单击SHIFT），对应的功能不变。
3、所有的轮询，尤其是线程中的轮询，全部修改成signalslot机制，尽量减少cpu消耗。

保持功能不变，不影响已有的所有功能，请做以上优化，目标是优化CPU消耗，将CPU消耗减少到最低。